default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  host: postgres
  user: postgres

development:
  <<: *default
  database: pomodoroClone_development

test:
  <<: *default
  database: pomodoroClone_test

production:
  <<: *default
database: pomodoroClone_production